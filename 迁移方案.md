# tygdq → newgdq (WPF) 迁移方案

> 目标：把 `tygdq/`（WinForms，添雨跟打器 v0.94，约 2.7 万行 C#）的功能"完全复刻"到 `newgdq/`（WPF）项目，**砍掉 QQ 相关 + 比赛相关功能**，并在过程中做合理的架构优化。
>
> 本文档不写代码，只交付**模块清单 + 依赖图 + 迁移策略 + 优化清单 + 分阶段实施计划**。代码在后续对话按阶段实施。

---

## 一、源项目盘点

### 1.1 解决方案结构

| 程序集 | 角色 | 核心内容 |
|---|---|---|
| `WindowsFormsApplication2` (`QQFollowingTypingApplication.csproj`) | 主程序 | 主界面、各功能窗体、业务逻辑 |
| `TyDll` | 基础控件库 | 自绘窗体基类、键盘钩子、Win32 封装、资源访问 |

主程序运行依赖：`Application.StartupPath\TyDll.dll` 必须存在（`Program.cs` 启动检查）。

### 1.2 核心文件体量（按行数排序，仅列 Top）

```
FormType.cs                  5476  ← 主跟打窗体（业务核心）
NativeMethods.cs             2873  ← Win32 封装（TyDll）
FormType.Designer.cs         2699  ← 主窗体设计器
Enums.cs                     1926  ← 枚举/钩子/虚拟键值
Setup.Designer.cs            1725  ← 设置窗体设计器
新发文.Designer.cs            1139
新发文.cs                      933  ← 文章源管理 + 发文（含 QQ 部分）
Setup.cs                      862  ← 设置窗体逻辑
SendTextStatic.Designer.cs    713
FormBase.cs                   582  ← 自绘无边框窗体基类
SendTextStatic.cs             445
FormTheme.cs                  306  ← 主题设置窗体
RichEditBoxLineRender.cs      329  ← 词组下划线渲染器（核心算法）
WordInfo.cs                   231  ← 词典/分词工具
TypeAnalysis.cs               215  ← 跟打报告
SpeedAn.cs                    188  ← 速度曲线
ShowMessage.cs                161  ← 浮动提示
SysTrayWnd.cs                 150
KeyHook.cs                    122  ← 键盘钩子（TyDll）
TextBoxEx.cs                  122  ← 多步撤销 TextBox
```

### 1.3 模块功能映射

| 模块目录 | 功能 | 是否保留 |
|---|---|---|
| `跟打部分/FormType` | 主跟打窗体（输入、计速、击键、码长、回改） | **保留（核心）** |
| `跟打部分/SpeedAn` | 实时速度曲线 | 保留 |
| `跟打部分/TextBoxEx` | 带撤销栈的输入框 | 保留（WPF 重写） |
| `跟打部分/RichEditBoxLineRender` | 对照区分词下划线 | 保留（WPF 重写为 Adorner） |
| `跟打部分/WordInfo` | 最长匹配分词 | **保留（纯算法直接搬）** |
| `跟打部分/JjCheck` | 击键检查浮窗 | 保留 |
| `跟打部分/ShowMessage` | 浮动提示 | 保留（WPF 用 Popup/Snackbar） |
| `跟打部分/SysTrayWnd` | 托盘窗口 | 保留 |
| `跟打部分/NewMenu`、`CustomProfessionalRenderer` | 自绘菜单 | 删（WPF 原生菜单+样式即可） |
| `跟打部分/Win32` | Win32 工具 | 部分保留（HwndSource 互操作） |
| `跟打部分/图片成绩设计/PicGoal_Class` | 成绩生成图 | 保留（WriteableBitmap/`RenderTargetBitmap`） |
| `发文重写/新发文` | 文章源选择面板 | 保留，**砍 QQ 发送** |
| `发文重写/SendTextStatic` | 静态发文器 | 保留，**砍 QQ 发送** |
| `发文重写/NewSendText` | 发文配置 | 保留 |
| `编码提示/FormBMTips` + `FormBMTipsModel` + `BmAll` + `State` | 编码提示（读 `bm.txt`） | **保留（核心）** |
| `跟打报告/TypeAnalysis` + `BaseFunc` | 报告 + 图片导出 | 保留 |
| `设置部分/Setup` | 全局设置（862 行，巨大） | **保留（拆分重做）** |
| `检查更新/UpgradePro` + `UpgradeModel` | 版本检查 | **删**（先不要，后续按需） |
| `精五成绩生成` | 比赛专用成绩 | **删（比赛）** |
| `SpeedCheckOut` / `SpeedCheckPoint` | 比赛测速点（`SendClipBoardToQQ`） | **删（比赛 + QQ）** |
| `捐助作者` / `AboutBox` | 关于页 | 保留（简化为一个 About 窗口） |
| `FormTheme` + `Theme` | 主题设置 | 保留（WPF 改为 ResourceDictionary 切换） |
| `Glob` (252 行 static) | **全局状态总线** | **重做：拆成多个领域模型 + 服务** |
| `_Ini` | INI 读写（`Ttyping.ty`） | 保留接口，实现换为 JSON 配置 |
| `MagneticMagnager` | 子窗口跟随主窗口（发文状态窗贴左、ShowMessage 贴底部） | 保留（WPF 实现仅需订阅 `Owner.LocationChanged/SizeChanged`，~30 行） |
| `NewButton` / `SwitchButton` / `ToolButton` / `NewForm` | 自定义控件/窗体 | **删**（用 WPF 样式替代） |
| `PerTyping.mdb` + `PerTypingDataSet` | Access 持久化 | **删，换 SQLite** |
| `TyDll/FormBase` + `KeyHook` + `NativeMethods` + `Enums` + `Structs` + `Consts` + `GetResources` | 基础库 | `KeyHook` + `NativeMethods` 保留；`FormBase` 删（WPF 用 `WindowChrome`） |

### 1.4 已识别的"必须删"清单（QQ + 比赛）

| 文件/符号 | 类别 |
|---|---|
| `Glob.isQQ`、`Glob.QQnumber` | QQ |
| `Glob.isMatch`、`Glob.jwMatchMoudle`、`Glob.AZpre`（精五随机段） | 比赛 |
| `精五成绩生成.cs` 整文件 | 比赛 |
| `SpeedCheckOut.cs`、`SpeedCheckPoint.cs` | 比赛 + QQ（`SendClipBoardToQQ`） |
| `Resources/QQ/` 全目录（`NewForm` 用到） | QQ（与 `NewForm` 一起删） |
| `FormType` 中：`转换为比赛文段ToolStripMenuItem`、`比赛时自动打开寻找测速点ToolStripMenuItem`、`从QQ窗口手动ToolStripMenuItem1`、`复制官方QQ群号ToolStripMenuItem`、`精五比赛成绩生成ToolStripMenuItem`、`SendClipBoardToQQ` 方法 | 二者皆删 |
| `Setup.cs` 中：`checkBox21`(QQSta)、`textBoxQQ`、`tbxName.Text`(QQ 昵称)、相关 ini 项 `发送/QQ`、`发送/QQSta` | QQ |
| `新发文.cs`：`HeaderFresh` 含从 QQ 群名提取段号；`comboBoxQQ` 等 | QQ |
| 所有 `Glob.isMatch == true` 分支 | 比赛 |

> **删除策略**：在迁移时直接不复刻，而非"先复刻后删"。这样目标项目里不留 dead code。

---

## 二、关键架构问题（这些是迁移的真正难点）

### 2.1 `Glob` 全局静态状态（252 行 static 字段）

**问题**：跟打过程中所有数值（速度、击键、码长、回改、当前段号、配置、颜色、字体、键数、左右手分布、跟打地图、报告……）都堆在一个 `static class Glob` 里，被几十个文件直接读写。这是迁移最大的耦合点。

**优化**：拆成
- `TypingSession`（一次跟打的运行时状态：当前文章、已打索引、键数、回改、起止时间……） → 实例化、可重置
- `TypingStats`（计算属性：速度、击键、码长、效率） → 从 `TypingSession` 派生
- `TypingHistory`（历史/报告） → 持久化到 SQLite
- `AppSettings`（INI/JSON 配置） → 用 `IOptions<T>` 模式
- `ThemeService`（主题）

WPF 用 MVVM 时，`TypingSession` 即 ViewModel 的核心字段，`INotifyPropertyChanged` 自动驱动 UI。**消除 Glob 后，UI 才能"脱钩"**。

### 2.2 `FormType.cs` 5476 行单文件

**问题**：主窗体既是 UI 又是业务逻辑容器，包含：键盘钩子处理、文章加载、计时、文本对比、统计、菜单事件、发文调度、托盘、热键、主题加载、跟打地图绘制……

**优化**：按职责拆分
- View：`MainWindow.xaml` + 几个 UserControl（对照区、跟打区、信息栏、状态栏）
- ViewModel：`MainViewModel`（命令绑定）
- Services：
  - `ITypingEngine`（输入比对、统计计算）
  - `IKeyHookService`（钩子封装为事件流）
  - `IArticleLoader`（文章源：内置、本地、剪贴板、自定义）
  - `IDictionaryService`（编码提示词典）
  - `IThemeService`、`ISettingsService`、`IHistoryRepository`

### 2.3 `RichEditBoxLineRender` 的 WPF 等价物

**问题**：WinForms 版用动态 `Label` 浮动控件画下划线（作者自承"性能上有点慢"），依赖 `RichTextBox.GetPositionFromCharIndex` 这种 Win32 API。

**WPF 方案**（优化点）：
- 对照区改用 `RichTextBox` + `FlowDocument`，下划线用 **`Adorner`** 一次性绘制
- 或更简单：直接用 `TextBlock` + `Inline.TextDecorations`（每个字段染色 + 下划线）
- 性能比"浮动 Label"好很多，且代码量减半

### 2.4 键盘钩子

`TyDll/KeyHook.cs` + `NativeMethods.cs` 是 WH_KEYBOARD_LL 全局钩子，**WPF 完全可以直接复用**（钩子不依赖窗体类型）。只需把 `KeyEventArgs` 从 `System.Windows.Forms.KeyEventArgs` 改成 `System.Windows.Input.KeyEventArgs`（或自定义 `KeyHookEventArgs`，避免 UI 框架耦合）。

### 2.5 持久化

WinForms 版同时使用：
- `Ttyping.ty`（INI 文件，`_Ini` 类，存绝大多数配置）
- `bm.txt`（编码词典，纯文本）
- `PerTyping.mdb`（Access 数据库，强类型 DataSet —— 实际使用面看起来很窄，需 P0 阶段确认）
- `Resources/TXT/*.txt`（内置文章）

**优化方案**：
- 配置：JSON（`appsettings.json`）+ 强类型 `AppSettings`
- 词典：保持 `bm.txt`（兼容现有用户文件）
- 历史/报告：SQLite（`Microsoft.Data.Sqlite`，免依赖 Office）
- 文章：保持文件系统

### 2.6 自绘窗体 / 控件

`TyDll/FormBase`（582 行，自绘标题栏+系统按钮+无边框拖动+缩放）和 `WindowsFormsApplication2/NewForm`、`NewButton`、`SwitchButton`、`ToolButton` 都是为了"QQ 风格皮肤"。

**WPF 等价物**：
- 无边框窗体：`WindowChrome` + `WindowStyle="None"` + `AllowsTransparency`
- 系统按钮：在标题栏 `Border` 里放 `Button`，绑命令 `SystemCommands.CloseWindow` 等
- SwitchButton：用 `ToggleButton` + `ControlTemplate`（10 行 XAML 搞定）
- 主题切换：`ResourceDictionary.MergedDictionaries` 替换

**全部删掉自定义控件 + 自绘窗体基类**，节省 ~1500 行。

---

## 三、目标项目（newgdq）结构提案

```
newgdq/
  newgdq.sln (新建，替换 .slnx)
  src/
    Newgdq.App/                 ← WPF 主程序
      App.xaml / App.xaml.cs
      MainWindow.xaml
      Views/
        TypingView.xaml         ← 主跟打区（对照+输入+信息栏）
        SpeedChartView.xaml     ← 速度曲线
        BmTipsView.xaml         ← 编码提示
        SendTextWindow.xaml     ← 发文窗口
        SettingsWindow.xaml     ← 设置（用 TabControl 分区）
        ReportWindow.xaml       ← 跟打报告
        ThemeWindow.xaml        ← 主题设置
        AboutWindow.xaml
      ViewModels/
        MainViewModel.cs
        TypingViewModel.cs
        SettingsViewModel.cs
        ...
      Controls/
        UnderlineDecorator.cs   ← Adorner，替代 RichEditBoxLineRender
        FloatTip.cs             ← 替代 ShowMessage
      Themes/
        Light.xaml / Dark.xaml / Default.xaml
      Resources/
        TXT/                    ← 内置文章（从 tygdq/Resources/TXT 拷过来）
    Newgdq.Core/                ← 业务逻辑（不依赖 WPF）
      Models/
        TypingSession.cs
        TypingStats.cs
        WordInfo.cs / WordInfoUtil.cs   ← 直接从 tygdq 搬
        TypeRecord.cs           ← TypeDate 改名
        BmEntry.cs              ← BmAll 改名
      Services/
        ITypingEngine.cs / TypingEngine.cs
        IDictionaryService.cs / DictionaryService.cs
        IArticleLoader.cs / ArticleLoader.cs
        ISettingsService.cs / JsonSettingsService.cs
        IHistoryRepository.cs / SqliteHistoryRepository.cs
        IThemeService.cs / ThemeService.cs
    Newgdq.Interop/             ← Win32 互操作
      KeyHook.cs                ← 从 TyDll 搬，去 WinForms 依赖
      NativeMethods.cs          ← 精简（删掉用不到的 ~80%）
  tests/
    Newgdq.Core.Tests/          ← 算法/计算单测
```

> **目标**：UI 项目里不写业务逻辑，业务项目不依赖 WPF。这样 `TypingEngine` 可单测，重构主界面不会牵动核心算法。

---

## 四、优化清单（迁移过程中顺手做掉的事）

| # | 优化点 | 收益 |
|---|---|---|
| O1 | 拆 `Glob` 为多个领域模型 | 解耦，可测，UI 自动响应 |
| O2 | 拆 `FormType` 5476 行为 MVVM 三段 | 可维护、可单测 |
| O3 | `RichEditBoxLineRender` → `Adorner`/`TextDecorations` | 性能 + 代码量 |
| O4 | INI → JSON | 类型安全、可读 |
| O5 | Access mdb → SQLite | 去 Office 依赖、跨平台潜力 |
| O6 | 自定义控件全删，用 WPF 样式 | 减少 ~1500 行 |
| O7 | `_Ini` 多次 `new` 改为单例服务 | 减少文件 IO |
| O8 | `Application.StartupPath` 全部替换为 `AppContext.BaseDirectory` | 去 WinForms 依赖 |
| O9 | `MessageBox.Show` 改为可注入的 `IDialogService` | 可测 |
| O10 | `KeyHook` 事件用 `Observable<KeyEvent>` 或 C# event，**不持有任何 UI 类型** | 解耦 |
| O11 | 删除版本检查模块 | 减少网络依赖 |
| O12 | `bool isQQ`、`isMatch`、`jwMatchMoudle` 等比赛/QQ 标志位**全删**（不留 dead config） | 干净 |
| O13 | `Dispatcher.Invoke` 替代 WinForms 的 `UIThread`/`Invalidate`，但只在跨线程时用 | 减少不必要的 invoke |
| O14 | 单元测试：`TypingEngine` 的"输入字符 → 速度/击键/码长"全部可测 | 防回归 |
| O15 | 文件命名英文化（仅新代码，不动用户配置）：`TypingSession`/`Settings`/`Report`，但保留中文 UI 文案 | 工程一致性 |

---

## 五、风险点与待澄清

1. **`PerTyping.mdb` 实际使用面有多大？** 从扫描看 `PerTypingDataSet` 在主流程被 `Glob.TextRecLenAll`、`Glob.TextRecDays`、`Glob.TypeCount` 等围绕，但需要 P0 阶段定位实际读写代码（`Form1` 内 `using ListTrayBarInfo;`）才能定 SQLite 表结构。
2. **比赛/QQ 与主流程的耦合深度**：`SendClipBoardToQQ`、`isMatch` 散落在 `FormType.cs` 各处。在 P1 阶段第一次看到对应代码时**直接跳过不复刻**，而不是先复刻再删。
3. **`MagneticMagnager`**（之前我误判了）：实际只是"子窗口跟随主窗口位置"（贴左/贴底/贴右等），不是边缘吸附拖动。WPF 直接订阅 `Owner.LocationChanged + SizeChanged` 重算 `Left/Top` 即可，~30 行。在 P3（ShowMessage 浮动提示）和 P4（发文窗口）顺手做掉。
4. **托盘 `SysTrayWnd`**：WPF 没有原生 `NotifyIcon`，需引第三方包（如 `H.NotifyIcon.Wpf`）。
5. **报告里的 `Bitmap` 画图**：可继续用 `System.Drawing`（.NET Framework 上没问题；如目标 .NET 6+ 需引 `System.Drawing.Common` 并接受 Windows-only 限制）。或改 `RenderTargetBitmap` 重写。
6. **目标 .NET 版本**：现 `newgdq.csproj` 是哪个 TFM？P0 第一步要确认。建议 **`net8.0-windows`**（长期支持 + WPF 全支持）。

---

## 六、分阶段实施计划

> 每阶段一次（或多次）独立对话，每阶段交付物**必须能编译 + 能跑 + 可演示**。

### P0 — 前置确认（半次对话，纯讨论）
- 确认 .NET 版本（建议 net8.0-windows）
- 确认 `newgdq.csproj` 现状（SDK 风格还是旧式？）
- 确认是否引入 SQLite、CommunityToolkit.Mvvm、H.NotifyIcon 等依赖
- 确认是否需要保留 `Ttyping.ty` INI 兼容（迁移老用户配置）

**验证**：用户拍板上述选型。

### P1 — 项目骨架 + 主窗口空壳
- 调整 `newgdq.csproj` 为目标 SDK
- 建 `Newgdq.Core` / `Newgdq.Interop` 项目并加引用
- WPF 主窗口：无边框 + WindowChrome + 系统按钮 + 一个空的对照区/输入区占位
- App 启动单实例 mutex（复刻 `Program.cs` 那段）
- 接入主题切换骨架（资源字典）

**验证**：能启动、能拖动、能关闭、能切换浅/深色。

### P2 — 跟打核心（最重，可能拆 2 次对话）
- `TypingSession` + `TypingStats` 模型
- `KeyHook` 移植（去 WinForms 依赖）
- `TypingEngine`：加载文章 → 比对输入 → 实时算速度/击键/码长/回改/键数/左右手
- View 绑定：实时显示数值
- `WordInfoUtil` 分词 + 下划线渲染（Adorner 方案）
- 内置文章载入（从 `Resources/TXT/`）
- **单元测试**：固定输入序列 → 期望统计值

**验证**：能加载一段文字、键盘输入、看到速度/击键/码长更新、输错变红、输对变正常色。

### P3 — 周边模块 1：编码提示 + 速度曲线 + 浮动提示
- `DictionaryService` 读 `bm.txt`
- 编码提示窗口
- 速度曲线（用 `LiveCharts2` 或 OxyPlot 替代 WinForms `Charting`）
- 浮动 tip

**验证**：跟打中实时显示编码提示和速度曲线。

### P4 — 发文 + 设置（拆 QQ/比赛）
- 发文窗口（自带文章 / 本地文件 / 剪贴板 / 自定义）
- 设置窗口（按 Tab 分区：外观、字体、颜色、热键、发送、显示）
- INI/JSON 配置切换 + 老 `Ttyping.ty` 一次性导入

**验证**：能选文章 → 加载到主窗口 → 设置生效。

### P5 — 跟打报告 + 历史持久化
- SQLite 表设计（每段成绩、每次跟打）
- 报告窗口（表格 + 图片导出）
- 主窗口 dataGridView 等价物（DataGrid）

**验证**：跟打完一段 → 写入 SQLite → 报告窗口能查看。

### P6 — 收尾
- 托盘
- About / 捐助页
- 热键注册（F3 重打、F4 获取等）
- 打包发布

**验证**：发布单文件 exe，能在另一台 Windows 跑起来。

---

## 七、不做清单（明确不复刻）

- 比赛模式 / 精五比赛成绩生成 / 测速点
- QQ 群名解析、QQ 发送、QQ 主题资源
- 检查更新（先不做）
- Access 数据库（直接换 SQLite）
- 全部自定义 WinForms 控件（用 WPF 样式替代）

---

## 八、本方案当前状态

- [x] 模块清单 + 删除项已识别
- [x] 架构问题 + 优化清单已列出
- [x] 分阶段计划已定
- [ ] **P0 待用户确认选型**（.NET 版本、依赖包、配置兼容策略）

下一步：用户回复 P0 选型 → 进入 P1 实施。
