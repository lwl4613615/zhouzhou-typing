<UserControl x:Class="newgdq.Views.SpeedAnalysisView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:sp="http://oxyplot.org/wpf"
        FontFamily="微软雅黑" FontSize="12"
        Foreground="{DynamicResource PrimaryTextBrush}">
    <DockPanel Margin="16" LastChildFill="True">
        <Border DockPanel.Dock="Top" Background="{DynamicResource PanelBG}" Padding="10" CornerRadius="3">
            <StackPanel>
                <TextBlock x:Name="TxtTitle" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource ValueFG}"/>
                <UniformGrid Columns="4" Margin="0,8,0,0">
                    <StackPanel><TextBlock Text="实际速度"    Foreground="{DynamicResource LabelFG}"/><TextBlock x:Name="TxtSpeed"  Foreground="{DynamicResource SpeedFG}" FontSize="16" FontWeight="Bold"/></StackPanel>
                    <StackPanel><TextBlock Text="错一罚五"    Foreground="{DynamicResource LabelFG}"/><TextBlock x:Name="TxtSpeed2" Foreground="{DynamicResource SpeedFG}" FontSize="16" FontWeight="Bold"/></StackPanel>
                    <StackPanel><TextBlock Text="理想速度*"   Foreground="{DynamicResource LabelFG}"/><TextBlock x:Name="TxtIdeal"  Foreground="{DynamicResource KpmFG}" FontSize="16" FontWeight="Bold"/></StackPanel>
                    <StackPanel><TextBlock Text="总用时(s)"   Foreground="{DynamicResource LabelFG}"/><TextBlock x:Name="TxtSec"    Foreground="{DynamicResource TimeFG}" FontSize="16" FontWeight="Bold"/></StackPanel>
                </UniformGrid>
                <TextBlock Foreground="{DynamicResource LabelFG}" FontSize="11" Margin="0,4,0,0"
                           Text="* 理想速度 = 假设所有事件都按 正常打字 速度完成的推算值"/>
            </StackPanel>
        </Border>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <ui:Button Content="复制" Width="80" Margin="6,0" Click="BtnCopy_Click"/>
        </StackPanel>

        <sp:PlotView x:Name="Plot" Margin="0,12,0,0" Background="Transparent"/>
    </DockPanel>
</UserControl>
